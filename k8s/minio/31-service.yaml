apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: mes
  labels:
    app: minio
spec:
  type: ClusterIP
  selector:
    app: minio
  ports:
    - name: http
      port: 8000
      targetPort: 8000