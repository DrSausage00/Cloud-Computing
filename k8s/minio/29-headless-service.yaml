apiVersion: v1
kind: Service
metadata:
  name: minio-headless
  namespace: mes
  lables:
    app: minio
  spec:
    clusterIP: None
    selector:
      app: minio
    ports:
      - name: api
        port: 9000